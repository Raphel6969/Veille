# v0.2.0 design-partner system signals (objective, pre-human)

Generated by `python -m scripts.design_partner_demo --matrix`.
These are OBJECTIVE signals the supervisor emits today. Partner
responses are captured separately via capture-template.md.

## Q3 — Repeated / cacheable tool calls

### expensive / all
- `search_competitors` x2 — CACHEABLE (idempotent=None, matches=['exact'])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
  summary: semantic_duplicates=0, cache_served=1, savings=$0.002

### expensive / optimize_active
- `search_competitors` x2 — CACHEABLE (idempotent=None, matches=['exact'])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
  summary: semantic_duplicates=0, cache_served=1, savings=$0.002

### expensive / plan
- `search_competitors` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
  summary: semantic_duplicates=0, cache_served=0, savings=$0.0

### expensive / memory
- `search_competitors` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
- `fetch_source` x2 — repeat (not cacheable) (idempotent=None, matches=[])
  summary: semantic_duplicates=0, cache_served=0, savings=$0.0

## Q1 — Cost tiers (when plan enabled)
- expensive/all: plan_tier=high_quality, total=$0.0218
- expensive/plan: plan_tier=high_quality, total=$0.0238
- success/all: plan_tier=high_quality, total=$0.0138
- failed_validation/all: plan_tier=high_quality, total=$0.0138

## Q5 — Routing (advisory, when plan enabled)
- expensive/research/researcher: model=None tier=high_quality reason=Selected mock-research for capability 'research' at tier high_quality.
- expensive/analysis/analyst: model=None tier=high_quality reason=Selected mock-analysis for capability 'analysis' at tier high_quality.
- expensive/writing/writer: model=None tier=high_quality reason=Selected mock-synthesis for capability 'synthesis' at tier high_quality.
- expensive/research/researcher: model=None tier=high_quality reason=Selected mock-research for capability 'research' at tier high_quality.
- expensive/analysis/analyst: model=None tier=high_quality reason=Selected mock-analysis for capability 'analysis' at tier high_quality.
- expensive/writing/writer: model=None tier=high_quality reason=Selected mock-synthesis for capability 'synthesis' at tier high_quality.
- success/research/researcher: model=None tier=high_quality reason=Selected mock-research for capability 'research' at tier high_quality.
- success/analysis/analyst: model=None tier=high_quality reason=Selected mock-analysis for capability 'analysis' at tier high_quality.
- success/writing/writer: model=None tier=high_quality reason=Selected mock-synthesis for capability 'synthesis' at tier high_quality.
- failed_validation/research/researcher: model=None tier=high_quality reason=Selected mock-research for capability 'research' at tier high_quality.
- failed_validation/analysis/analyst: model=None tier=high_quality reason=Selected mock-analysis for capability 'analysis' at tier high_quality.
- failed_validation/writing/writer: model=None tier=high_quality reason=Selected mock-synthesis for capability 'synthesis' at tier high_quality.

## Q4 — Freshness defaults observed
- cache default TTL = 300.0s; memory default TTL = None

## Q2 — Intervention explanations
- expensive/all: applied=2, triggered=2
- expensive/optimize_active: applied=2, triggered=2
- expensive/plan: applied=2, triggered=2
- expensive/memory: applied=2, triggered=2
